# Feature List

Tracks progress against the constitution's Development Workflow gate:
every feature below needs a complete `spec.md`, `plan.md`, and `tasks.md`
before implementation begins on **any** of them (v0.3.0-draft). This is a
living tracking doc, not itself a spec — update it as wireframes land and
as `/speckit-specify`/`/speckit-plan`/`/speckit-tasks` are run per feature.

**Status key**: 🎨 wireframe pending · ✅ wireframed, not yet spec'd ·
📝 spec/plan/tasks in progress · 🟢 ready (spec+plan+tasks all complete) ·
🚀 implemented (merged to `main`, all tasks done)

## Exempt from the gate (infrastructure, not product surface)

- **Design System / shared UI primitives** — nav shell (🚀 **built**
  2026-07-14, see `status.md`'s "Real global nav shell built" section —
  `site-header.tsx`/`nav-links.tsx`/`profile-menu.tsx`, replacing the
  bare logo+bell slot every prior feature had deferred to), admin shell,
  buttons, cards, tags, toggles, and the loading/error patterns (skeleton
  loading, fetch-error state, pending-submit buttons — see `status.md`'s
  fourth gap-analysis pass; this was briefly in `guidelines.md` §4.6 but
  dropped by the latest regeneration, see note below). Built directly,
  like the initial app/db/auth scaffold, per the constitution's
  infrastructure-only exception.

## Wireframed, ready to spec

1. Auth & Onboarding — 🚀 **implemented** (`specs/001-auth-onboarding/`, branch `001-auth-onboarding` merged to `main`, `playm8z - Auth & Onboarding.dc.html`) — all 40 tasks across Setup/Foundational/US1(P1)/US2(P2)/US3(P3)/Polish complete: sign-up/login UI, the 4-step onboarding wizard, email verification (console-log fallback pre-domain), and the unverified-email write-action gate helper. First feature actually built — the project-wide spec/plan/tasks gate only blocked implementation from *starting*, not from proceeding feature-by-feature once cleared.
2. Error Pages (404/500/403/maintenance) — 🚀 **implemented** (`specs/002-error-pages/`, branch `002-error-pages` merged to `main`, `support/playm8z - Error Pages.dc.html`) — all 23 tasks complete: one shared `error-state.tsx` driving Next.js 16's native `not-found.tsx`/`error.tsx`+`global-error.tsx`/`forbidden.tsx`+`unauthorized.tsx`, a new read-only `settings` table for the maintenance flag (interim manual toggle via `db:studio`/SQL pending Admin Settings), `require-role.ts` (ready but honestly forbids everyone above "user" rank until Admin Settings adds the real `role` column), and a `proxy.ts` maintenance short-circuit exempting `/admin/*`.
3. Home — 🚀 **implemented** (`specs/003-home/`, branch `003-home` merged to `main`, `playm8z - Home.dc.html`) — all 24 tasks complete: `/` now redirects unauthenticated visitors to `/login` and otherwise renders the real search-first discovery page (live search/filter/sort over a server-fetched open-postings list, a recalculated-per-load Trending row, and an empty state with a "Post this game" CTA). New minimal `postings` table (Post a Game will extend it); a dev-only `npm run db:seed-postings` script stands in until that feature exists. Caught and fixed a real WCAG AA contrast bug in the Serious/vibe tag (bold 11px magenta-on-tint doesn't qualify for the "large text" exception) by adding a `--color-pop-text` design token. Amended (2026-07-13, ADR 0006): listing cards show the host's `@handle`, never their real/display name.
4. Browse — 🚀 **implemented** (`specs/004-browse/`, branch `004-browse` merged to `main`, `playm8z - Browse.dc.html`) — all 22 tasks complete: full server-side faceted search/filter/sort over the `postings` table (extended with `genre`/`ageGroup`/`timeSlots`/`platform`/`micRequired`/`scheduledDate`), live Game/Region facet counts, removable filter pills with "Clear all," and an empty state. Caught and fixed a real Postgres bug — `get-facet-counts.ts`'s two queries omitted a `users` join their shared `buildFilterConditions()` helper could reference, causing a live 500 whenever a keyword search was active. Also fixed a flaky axe-core heading-order finding by removing unneeded `<Suspense>` boundaries around client components on an already-fully-dynamic page.
5. Post a Game — 🚀 **implemented** (`specs/005-post-game/`, branch `005-post-game` merged to `main`, `playm8z - Post a Game.dc.html`) — all 20 tasks complete: the `/post` listing-creation form (Server Action, live preview reusing the shared `listing-card.tsx`, Group size/Spots-open stepper clamping re-validated server-side), gated on authentication (redirect to `/login`) and — as the first real consumer — Auth & Onboarding's unverified-email write gate. Extends `postings` with `tags`/`recurring`/`voiceLink`, and loosens `genre` to nullable (Browse's original NOT NULL never accounted for genre being optional to publish). Fixed the same heading-order a11y issue class Browse hit by promoting the form's section labels to real `<h2>`s.
6. Listing detail — 🚀 **implemented** (`specs/006-listing-detail/`, branch `006-listing-detail` merged to `main`, `playm8z - Listing.dc.html`) — all 27 tasks complete (T030 landed 2026-07-13 as a bounded amendment alongside `012`): the public `/listing/[id]` page (apply/withdraw, a public Q&A thread with host-only replies, a derived roster with no role labels per ADR 0004, Share/Save/Report). Introduces `applications` and `questions` (first real writes to either) and `savedListings` (Profile's, `007`, entity — built here first since this feature needs it before Profile is implemented). Second and third real consumer of Auth & Onboarding's unverified-email write gate. Found and fixed a real, reproducible dev-mode-only Playwright false alarm (confirmed via a real `next build`/`next start` check that production sends a proper `no-store` header) — see `status.md`.
7. Profile + Account settings — 🚀 **implemented** (`specs/007-profile-and-account-settings/`, branch `007-profile-and-account-settings` merged to `main`, `playm8z - Profile.dc.html`) — all 35 tasks complete: four real routes under `/profile` (Overview, My postings, Saved, Account) sharing one layout. Extends `user` with `bio`/`createdAt`/four privacy booleans/`deactivatedAt`; adds `userGames` (richer than onboarding's flat game list). Reuses (doesn't duplicate) Post a Game's posting-edit schema and Listing detail's `toggleSavedListing`/`savedListings`. `src/auth.ts`'s second amendment (after Auth & Onboarding's Google callback): a `signIn` callback auto-reactivates a deactivated account. Bounded amendment to Home/Browse: their queries now exclude a deactivated host's postings (FR-013). Caught and fixed a real, previously-latent Vitest file-parallelism race condition (`vitest.config.ts` now serializes test files, mirroring `playwright.config.ts`'s own `workers: 1`) — see `status.md`.
8. Blocked Users — 🚀 **implemented** (`specs/008-blocked-users/`, branch `008-blocked-users` merged to `main`, `support/playm8z - Blocked Users.dc.html`) — all 20 tasks complete: `/profile/account/blocked` (live count, client-side search over the fetched list, both empty states) plus this project's first modal-dialog UI, built on the native `<dialog>` element (`showModal()`/`close()`) rather than any library — a reusable two-step Block modal (pick candidate → confirm, optional "Also report") and a one-step Unblock confirm dialog, both focus-trapped for free. New `blocks` table (never hard-deleted per ADR 0005 — `unblockedAt` instead, since a block has real trust/safety history) and `reports` (this feature's first writer, `targetType='user'` only — the not-yet-built Notifications + Report feature owns every other write path and all moderation UI). Both write actions extend Auth & Onboarding's `requireVerifiedEmail()` gate; candidate search only needs `requireAuth()` since searching isn't itself a block/unblock. Found and fixed a real, previously-unnoticed accessibility bug shared with Listing detail's own breadcrumb pattern (`text-muted` link text inside `text-dim` surrounding text fails axe's link-in-text-block contrast/distinguishability check) by underlining this feature's own breadcrumb link — see `status.md`.
9. Forum index — 🚀 **implemented** (`specs/009-forum-index/`, branch `009-forum-index` merged to `main`, `playm8z - Forum.dc.html`) — all 22 tasks complete: `/forum`, public to read, with server-side URL-driven category/search/sort (Browse's precedent, since threads accumulate indefinitely), pinned-always-first ordering, a HOT badge computed at read time (never stored, never alongside PINNED), a right rail with real member/thread counts and trending tags, and a New Thread modal reusing Blocked Users' native-`<dialog>` pattern (its own component, not a shared one — different fields). New `forumThreads` table, this feature's first writer (categories are a hardcoded six-key const, not a table). `create-thread.ts` extends Auth & Onboarding's `requireVerifiedEmail()` gate; an unauthenticated visitor is routed to `/login` instead of ever opening the modal (Listing detail's apply-panel.tsx precedent). Caught and fixed a genuine test-authoring race (clicking two different URL-updating controls back-to-back without letting the first settle let a debounced update rebuild the URL from a stale snapshot) — see `status.md`.
10. Forum Thread — 🚀 **implemented** (`specs/010-forum-thread/`, branch `010-forum-thread` merged to `main`, `playm8z - Forum Thread.dc.html`) — all 28 tasks complete: `/forum/thread/[id]`, public to read, with a distinctly-styled OP, client-side Top/Newest/Oldest reply sort (a single thread's own reply count is bounded, unlike Forum index's ever-growing cross-category list), quoting, per-user like/unlike (a real `likes` table with a database-level unique constraint, not a bare counter, so a race can't double-count), reporting (this feature's second writer of Blocked Users' `reports`, `targetType='forum'`), and a per-user thread-subscription preference (no notification delivery wired up yet). New `forumReplies`/`likes`/`threadSubscriptions` tables. Every write action (reply/like/report/subscribe) extends `requireVerifiedEmail()`; an unauthenticated visitor is routed to `/login` via real links throughout, not client-side redirects. Dropped the wireframe's "TOP REPLY"/best-answer badge entirely — nothing sets it, unlike the real, kept "Top" (by-likes) sort.
11. Inbox / messaging — 🚀 **implemented** (`specs/011-inbox-messaging/`, branch `011-inbox-messaging` merged to `main`, `playm8z - Inbox.dc.html`) — all 31 tasks complete: a two-pane `/inbox` (conversation list + `/inbox/[id]` chat pane) merging real `conversations` with pending-hosted `applications` into one searchable list, group/direct messaging with sender-grouped bubbles, a compose flow (direct conversations reuse an existing one; 2+ recipients create a group), and host Accept/Decline on pending join requests. New `conversations`/`messages` tables — `conversations.lastReadAt` (a per-member JSON read-cursor, added beyond data-model.md's original two-column sketch) is the actual mechanism behind accurate unread counts. `accept-request.ts` is this project's first `db.transaction()`, guarded by a `.returning()`-checked update so a concurrent double-accept can't double-decrement `seatsOpen` or create two conversations (verified with a real concurrent-call test, unlike the toggle-race antipattern Forum Thread's own lesson warned against — accept is a one-way transition, not a toggle, so concurrent calls here are a valid test). `search-contacts.ts` excludes blocked/blocking users in *either* direction (Blocked Users' own candidate search only excludes one direction), and `start-conversation.ts` re-checks the same relationship server-side. Found and fixed three real bugs: (1) a native `<dialog>` styled with a bare Tailwind `flex` utility never actually hides when closed, because Tailwind v4 emits utilities inside `@layer`, and per the CSS cascade-layers spec, layered author styles always beat the UA stylesheet's `dialog:not([open]) { display: none }` regardless of specificity — fixed via `hidden open:flex`, a latent bug likely also present in Blocked Users'/Forum index's own dialogs, left unfixed there (out of this feature's scope) but worth a follow-up; (2) this Next.js version's client `router.refresh()` called immediately after `router.push()` can lose a race and revert navigation to the pre-push URL — confirmed via server logs showing the target route's fetch succeeding while the browser still showed the old URL; fixed by moving cache invalidation server-side into the Server Actions themselves via `revalidatePath(path, "layout")` (this Next.js version's documented pattern, `next/cache`'s `refresh()`/`revalidatePath` are Server-Action-only now), with client code doing a bare `router.push()` and nothing else; (3) a Playwright `getByText()` false-positive matched a composer `<textarea>`'s still-pending typed value before its async send resolved — fixed by waiting for the textarea to clear first.
12. Notifications + Report modal — 🚀 **implemented** (`specs/012-notifications-and-report-modal/`, branch `012-notifications-and-report-modal` merged to `main`, `support/playm8z - Notifications & Report.dc.html`) — all 25 tasks complete: a nav-level bell dropdown plus a full `/notifications` page (All/Unread/Requests/Forum/System filters, Today/Earlier grouping, mark-read/mark-all-read, empty state), and a reusable 3-step Report modal (reason → optional details + "Also block" → done). New `notifications` table and a `createNotification()` helper — deliberately shipped with no live callers yet (each already-existing write action's own retrofit is tracked in `docs/future-work.md`); demonstrated instead against seeded data plus a genuinely live source: pending/resolved join-request `applications` are synthesized into "request" items exactly like Inbox's own merged list, except this feed also keeps `accepted`/`declined` ones around (not just `pending`) so a resolved request still shows its resolved state instead of vanishing. Accept/Decline on a request notification call Inbox's (`011`) existing `accept-request.ts`/`decline-request.ts` directly — no duplicated transaction logic. Extends Blocked Users' (`008`) `reports` table with its first real `reason` values and a new `details` column (data-model.md's original sketch never accounted for persisting the report flow's free-text details); `submit-report.ts` decouples "who's being reported" from "who gets blocked" via an explicit `blockUserId`, since reporting a posting blocks its *host*, not the posting id itself. Retroactively un-defers Listing detail's (`006`) previously-blocked Report action (T030): the apply panel reports the posting itself (blocking the host), each Q&A entry reports its asker directly. Found and fixed three real issues: (1) no shared nav shell exists anywhere in this codebase — every prior feature deferred it as future Design System infra and simply rendered none — so this feature had to create the smallest possible slot itself (a thin global `SiteHeader`, logo + bell only, mounted in the root layout) to satisfy "the bell must be reachable from anywhere," and had to shrink Inbox's own fixed `h-screen` two-pane layout by the new header's height so it doesn't overflow the viewport; (2) a client component (`notifications-list.tsx`) imported a runtime function from the same module that also exported the DB-touching `getNotifications()`, pulling the `postgres` driver into the browser bundle and crashing every page — fixed by splitting the pure, DB-free filter/grouping logic and types into their own `filter-notifications.ts`, importable safely from client code (a type-only import, as Inbox's own `conversation-list.tsx` already did for `InboxItem`, would have avoided this, but a runtime function import doesn't get the same free pass); (3) the same axe-core color-contrast class Inbox already hit once (`text-dim` on an accent-tinted selected background, 4.37:1 vs 4.5:1) — fixed with `text-muted`.
13. News feed (public) — 🚀 **implemented** (`specs/013-news-feed/`, branch `013-news-feed` merged to `main`, `playm8z - News.dc.html`) — all 19 tasks complete: the public `/news` page (no login required), a single featured post shown only with no active category/search, server-side URL-driven category filter + debounced search (Browse/Forum's precedent), cumulative "Load more" pagination, and a no-account-required newsletter subscribe strip. New minimal `newsPosts` table (Home/Post-a-Game-style — the future Admin News feature extends it) and `newsletterSubscribers` — this project's first write action with genuinely no session check at all, guarded only by a database-level unique constraint on `email`. Found and fixed a real, previously-latent bug shared with three already-merged features (`toggle-like.ts`, `toggle-subscription.ts`, Auth & Onboarding's register route, Profile's `update-email.ts`): their shared `isUniqueViolation()`-style helper checked `err.code` directly, but Drizzle wraps the raw postgres.js error in a `DrizzleQueryError` whose own `code` is undefined — the real code lives at `err.cause.code` — so every one of those catch branches had silently never worked; fixed consistently across all five call sites. Also found and fixed a second latent bug: two sibling dev-only seed scripts with no top-level import/export both declared `main()` in the same TypeScript global scope and collided the moment a second one existed — fixed by making both explicit ES modules (`export {}`).
14. Content Page (public render + inline admin edit) — 🚀 implemented (2026-07-14) — 21 tasks across Setup/Foundational/US1(P1) public-read/US2(P2) inline-edit/US3(P3) publish+unpublish/Polish, all complete. `require-role.ts`'s first real consumer; its rank check is still hardcoded to reject every session (no `role` column until Admin Settings/024), so US2/US3 are real, shipped code paths that can't yet be exercised by an actual moderator session — covered instead by mocking `requireRole` in integration tests, with e2e covering US1 fully plus confirming a logged-in non-moderator sees no edit controls and is 404'd for drafts same as an anonymous visitor
15. Admin Dashboard — 🚀 implemented (2026-07-14) — all 24 tasks across Setup/Foundational/US1(P1) KPIs+chart+top-games/US2(P2) needs-attention+activity/Polish complete: `/admin`'s main content area (the sidebar shell remains Design System infra, not yet built), five real-count KPI cards, a 7-day activity chart with an accessible metric switcher (real ARIA tab semantics, a visually-hidden `<table>` as the chart's non-visual data equivalent), Needs-attention (three `reports`-table queues by `targetType`), a recent-activity feed backed by a new `auditEntries` table, and Top games (a thin re-export of Home's/Browse's existing Trending query). `requireRole("moderator")` gates the whole route — its second real consumer after Content Page (014) — so, like that feature's own US2/US3, the KPI/chart/needs-attention/activity content can't be exercised by a real session yet (no `role` column until Admin Settings/024); every `lib/admin/*.ts` query is unit-tested directly instead, and e2e covers the real, current access-denial behavior for both an unauthenticated visitor and a logged-in non-moderator. `logAuditEntry()` ships fully built and tested with no callers wired up yet — Admin Users/Postings/Forum/News (all already spec'd, none yet implemented) will adopt it once built.
16. Admin Users — 🚀 implemented (2026-07-14) — all 27 tasks across Setup/Foundational/US1(P1) view+search+filter/US2(P2) ban+unban/US3(P3) drawer+content-removal/Polish complete: `/admin/users`'s stats (total/active/flagged/banned), a real server-side searchable/filterable table (computed "flagged" — an unbanned user with an open `user`-targeted report, never a stored value), Ban/Unban (the single severe account action, no separate Delete — the exact Profile Deactivate-vs-Delete resolution reapplied), and a per-user detail drawer (native `<dialog>`, real ARIA tab semantics, Postings/Forum-posts tabs) with a real, effect-having Remove action. Extends `user` with `bannedAt` and `postings`/`forumThreads` with `removedAt`; bounded amendments to Home's `get-open-postings.ts`, Browse's `search-postings.ts` (`buildFilterConditions`), and Forum index's `search-threads.ts` now exclude removed rows (verified both via new unit tests and a real Browse/Forum-index check during QA). `requireRole("moderator")` gates the route and both Server Actions independently — its third real consumer after Content Page (014) and Admin Dashboard (015) — so, same as those two, the real table/ban/drawer content can't be exercised by a real session yet (no `role` column until Admin Settings/024); every query and action is unit/integration-tested directly (with before/after-delta assertions for stats touching shared global tables), and e2e covers the real, current access-denial behavior. Found and fixed a real bug during QA: both new Server Actions called `revalidatePath("/admin/users")` without the `"layout"` type argument — the same stale-UI-after-Server-Action class of bug this project already hit and fixed once for Inbox — caught via a manual ban/unban click-through, not by the automated suite (which can't exercise a real session at all yet).
17. Admin Postings — 🚀 implemented (2026-07-14) — all 31 tasks across Setup/Foundational/US1(P1) view+filter queue/US2(P2) drawer+approve+remove/US3(P3) warn+ban/cross-feature amendments/Polish complete: `/admin/postings`'s stats (in queue/user-reported/auto-flagged/removed today), a queue combining reported and auto-flagged postings under one queue-membership formula (a posting with both counts as "User-reported" only, never double-filed), a computed-not-stored severity band (worst of every open report's reason-implied severity and the posting's auto-flag reason's own fixed severity), and a per-posting review drawer (why it's here, author card with prior-warnings/total-posts) with Approve/Remove/Warn/Ban. New deterministic, non-learned auto-flag ruleset (`src/lib/postings/auto-flag.ts` — fixed scam/boosting regex lists plus a new-account-first-post check) wired into Post a Game's (`005`) `create-posting.ts`. New `warnings` table (this feature's only writer) and `postings.autoFlagReason`/`moderationReviewedAt` columns. Ban delegates to Admin Users' (`016`) existing `toggleUserBan` directly (careful never to accidentally un-ban an already-banned author) then removes the posting under review via the same path Remove uses — banning someone without removing the content that justified it would leave that content still live. `resolvePostingReport`/`banPostingAuthor` are this feature's — and the whole project's — first real callers of `logAuditEntry()` (`015`), also retroactively wiring Admin Users' own previously-unwired `toggleUserBan`/`removeUserContent` to it. Also retroactively fixed a real over-count bug in Admin Dashboard's `get-dashboard-kpis.ts` and Home's/Browse's `get-trending.ts` (both "Live postings"/Trending queries were missing the same `removedAt IS NULL` exclusion Admin Users' own amendments already added elsewhere). `requireRole("moderator")` gates the route and both new Server Actions independently — its fourth real consumer after Content Page (`014`), Admin Dashboard (`015`), and Admin Users (`016`) — so the real queue/drawer/resolution content can't be exercised by a real session yet (no `role` column until Admin Settings/`024`); every query/action is unit/integration-tested directly, and e2e covers the real, current access-denial behavior. Visual QA (temporary local bypass across the page and all three gated Server Actions, since `banPostingAuthor` also transitively re-triggers `toggleUserBan`'s own independent gate) exercised all four resolution actions end-to-end with no bugs found — confirmed via direct DB checks that reports resolve, `warnings`/`auditEntries` rows land correctly, a removed posting disappears from Browse, and Ban both bans the account and removes the posting under review.
18. Admin Forum — 🚀 implemented (2026-07-15) — all 41 tasks across Setup/Foundational/US1(P1) view+filter queue/US2(P2) drawer+approve+remove/US3(P3) lock+warn+ban/cross-feature amendments to 009+010/retroactive amendments to 017/Polish complete: `/admin/forum`'s stats (in queue/user-reported/auto-flagged/actioned today — the last a live read of `auditEntries`, not a stored counter), a queue spanning threads AND replies under the same queue-membership formula as Admin Postings (`017`), filterable All/Threads/Replies/Auto-flagged, and a review drawer showing the flagged content in context (a reply's immediately-preceding message dimmed above it, falling back to the thread's own OP when the reply is the thread's first) with Approve/Remove/Lock (threads only)/Warn/Ban. Extracted two shared helpers out of `017`'s own inline copies — `src/lib/moderation/reason-severity.ts` and `auto-flag-rules.ts` — the exact "generalize once a second real consumer exists" trigger `017`'s own research.md anticipated; also generalized `017`'s posting-specific `warnings.postingId` to a polymorphic `targetType`/`targetId` pair (the third-distinct-source trigger `017`'s research.md separately anticipated), so "prior warnings" now correctly combines across postings/threads/replies in one count. Reuses Forum Thread's (`010`) existing `reports` usage (`targetType='forum'`, classified against `forumThreads` then `forumReplies` since that table never added a discriminator column) and Admin Users' (`016`) `toggleUserBan`/`forumThreads.removedAt` directly. Adds a new `forumReplies.removedAt` (`016` never extended that table) and `autoFlagReason`/`moderationReviewedAt` on both tables; "Lock thread" reuses `forumThreads`' existing `locked` boolean (Forum index's own comment had already reserved it for "the future Admin Forum feature") rather than adding a redundant `lockedAt` timestamp the spec's own data-model.md had sketched before checking the column already existed. Small bounded amendments wire the shared auto-flag ruleset into Forum index's `create-thread.ts` and Forum Thread's `post-reply.ts` (which also now rejects replying to a locked thread, re-verified server-side per Principle II — confirmed via a real browser: the composer still renders but submission is server-rejected with a visible error) and exclude removed replies from `get-thread.ts`'s reply list. `requireRole("moderator")` gates the route and all three new/reused Server Actions independently — its fifth real consumer after Content Page (`014`), Admin Dashboard (`015`), Admin Users (`016`), and Admin Postings (`017`) — so the real queue/drawer/resolution content can't be exercised by a real session yet (no `role` column until Admin Settings/`024`); every query/action is unit/integration-tested directly, and e2e covers the real, current access-denial behavior. Visual QA (temporary local bypass across the page and all three gated Server Actions) exercised all five resolution actions end-to-end with no bugs found, confirmed via direct DB checks and the real Forum index/Forum Thread pages.
19. Admin Reports — 🚀 implemented (2026-07-15) — all 37 tasks across Setup/Foundational/US1(P1) view+filter grouped queue/US2(P2) dismiss+remove/US3(P3) warn+ban/cross-feature amendment to 011/retroactive amendments to 017+018/Polish complete: `/admin/reports`'s unified triage queue across postings, forum, profiles, and messages — its own stats (open reports/high priority/resolved today/avg response, the last two the first live read of a new `reports.resolvedAt`), a queue grouped by reported TARGET rather than one row per report (`getReportsQueue` groups every open report by `(targetType, targetId)`, using the earliest-filed open report as the representative reporter/note and a "N reports" badge for the rest — a deliberate breadth-over-depth simplification versus `017`'s/`018`'s own per-report drawers), filterable All/Postings/Forum/Profiles/Messages, and a review drawer (representative reporter + "+N others reported this," reported content in context, a working "Open in [module] moderation →" cross-link to `017`'s/`018`'s/`016`'s own dedicated queues where one exists — never for messages, which have none) with Dismiss (a new generic, any-target-type action)/Remove/Warn/Ban. For postings and forum targets, Remove/Warn/Ban all DELEGATE to `017`'s/`018`'s existing resolution actions (via a new shared `classify-forum-target.ts` for the thread-vs-reply split) rather than reimplementing — guaranteeing identical effects regardless of entry point. Profiles and messages have no prior dedicated queue, so this feature is their first real mover: a new `messages.removedAt` (with a bounded amendment to Inbox's `011` conversation-view query excluding it) and Warn/Ban writing directly to the generalized `warnings` table with `targetType='message'` or `null` (profile reports carry no separate content id). "Total reports" (drawer's owner card) is a computed, all-time, cross-source aggregate — direct profile reports plus every report against any posting/thread/reply/message the user authored — never a maintained counter. Two small, bounded, single-place retroactive fixes ripple to every moderation feature at once: `017`'s and `018`'s resolve actions now also set `reports.resolvedAt` alongside `status`, and the shared `reason-severity.ts`'s `impersonation` mapping corrected from medium to high (the wireframe's own seed data treats a phishing-adjacent impersonation case as a real security risk, not routine) — both changes visible on `017`'s/`018`'s own queues too, without touching either feature's files beyond that one line. `requireRole("moderator")` gates the route and all three new Server Actions (dismiss/resolve/ban) independently — its seventh real consumer after Content Page (`014`), Admin Dashboard (`015`), Admin Users (`016`), Admin Postings (`017`), and Admin Forum (`018`) — so the real queue/drawer/resolution content can't be exercised by a real session yet (no `role` column until Admin Settings/`024`); every query/action is unit/integration-tested directly, and e2e covers the real, current access-denial behavior. Visual QA (temporary local bypass across this feature's own gates plus Admin Forum's/Admin Postings'/Admin Users' own page gates, since the cross-link navigates into them for real) exercised grouping, severity correction, filtering, the cross-link, and Dismiss/Remove/Warn/Ban across all four target types end-to-end with no bugs found beyond QA-script locator issues (a posting/thread title is deliberately never rendered on its own queue card, matching `018`'s own precedent) — confirmed via direct DB checks and the real Inbox page that a removed message disappears from its conversation while an untouched sibling message still shows.
20. Admin News — 🚀 implemented (2026-07-15) — all 26 tasks across Setup/Foundational/US1(P1) view+filter list/US2(P2) editor+publish+schedule+draft/US3(P3) pin+delete/cross-feature amendment to 013/Polish complete: `/admin/news`'s two-pane CMS — a filterable (All/Published/Drafts/Scheduled) post list with cover thumb, status badge, date, and pin indicator, and an editor (cover color swatches, title, category chips, excerpt, a markdown-snippet-assisted body textarea, publish settings) with a live preview that tracks every field change before saving. This feature is News feed's (`013`) first real `NewsPost` writer — adds `body` (plain markdown) and `status` (`draft`\|`published`\|`scheduled`). One `save-news-post.ts` Server Action handles create/update, branching on a discriminated `action` (publish/schedule/save-draft/delete) rather than five near-identical actions: `publish` only sets `publishedAt=now()` when the row isn't already published (an edit to a live post via "Update" never re-dates it); `save-draft` always overrides to `draft` regardless of the editor's on-screen status selection; `delete` is the ADR-0005-safe "Unpublish" (`status→draft`, row never removed). Reuses `013`'s existing `featured` column for "pin," enforcing the at-most-one-featured invariant `013`'s own data-model.md had deferred to this feature, in the same transaction as every save (pin state travels with whichever footer action is clicked, not its own separate save). Small, bounded amendment to `013`'s `search-news.ts` (plus its featured-post pick) so the public feed only shows posts that are published, or scheduled with a publish date/time that has passed — computed at read time, no background job; `scripts/seed-news-posts.ts` and `e2e/news-feed.spec.ts` updated to seed `status: "published"` explicitly, since the schema default is `draft`. `requireRole("moderator")` gates the route and the one Server Action — its eighth real consumer after Content Page (`014`), Admin Dashboard (`015`), Admin Users (`016`), Admin Postings (`017`), Admin Forum (`018`), and Admin Reports (`019`) — so the real list/editor content can't be exercised by a real session yet (no `role` column until Admin Settings/`024`); every query/action is unit/integration-tested directly, and e2e covers the real, current access-denial behavior. Visual QA (temporary local bypass across the page and the one Server Action, fully reverted) exercised create+publish, update-preserves-publishedAt, schedule, save-draft-override, pin-exclusivity, and delete-as-unpublish end-to-end with no product bugs found — every QA-script failure was a locator ambiguity from a real but harmless UI coincidence ("Update"/"Published"/"Scheduled" are simultaneously a NEWS_CATEGORY chip label, a list filter-chip label, AND the editor's own status-control/primary-button label), not a defect.
21. Admin Content Pages — 🚀 implemented (2026-07-14) — all 25 tasks across Setup/Foundational (incl. system-page seed)/US1(P1) view+search+filter/US2(P2) publish+unpublish+create/US3(P3) delete/Polish complete: `/admin/content-pages`, a thin management list wrapping Content Page's (`014`) already-existing `ContentPage` table — stats (total/published/drafts/system), a fetch-all-then-filter search (title/slug) + status/system filter (research.md's own small-bounded-list precedent, Admin News' `020` not Admin Users'/Browse's SQL-paginated one), and row actions. Publish/Unpublish call `014`'s existing `toggle-page-status.ts` directly (no second implementation); View/Edit both navigate to the page's own public slug, where `014`'s existing inline-edit affordance already lives — this feature builds no second content-editing UI. Delete (custom pages only, inline "Delete? Yes/No" with real focus management, Admin Users' own established pattern) is a thin new `delete-content-page.ts` that unconditionally sets `status='draft'` (ADR 0005, never a row removal) and rejects `system=true` targets server-side as defense in depth beyond the UI's own gate. "+ New page" (`create-content-page.ts`) generates a unique `untitled-page`/`untitled-page-2`/… slug. Adds `contentPages.system` (new boolean column) and this feature's own Foundational-phase seed (`scripts/seed-system-pages.ts`, idempotent) inserting About Us/Privacy Policy/Terms of Use as real, published, `system=true` rows — no prior feature ever wrote a `ContentPage` row, so a fresh deployment needed this to launch with real legal pages, not zero. Found and fixed a real data-model.md inconsistency before it shipped: the spec's own seed-data table listed slugs with a leading slash (`/about`) matching the wireframe's cosmetic display, but `014`'s real `contentPages.slug` convention (its own seed script, e2e spec) stores bare slugs matched against the `/pages/[slug]` dynamic route — a stored leading slash would never route; corrected to bare slugs (`about`/`privacy`/`terms`) in both the seed script and `create-content-page.ts`'s own slug generator, with the leading slash added back only for cosmetic display in the admin table. `requireRole("moderator")` gates the route and both new Server Actions independently — its ninth real consumer after Content Page (`014`), Admin Dashboard (`015`), Admin Users (`016`), Admin Postings (`017`), Admin Forum (`018`), Admin Reports (`019`), and Admin News (`020`) — so the real list/action content can't be exercised by a real session yet (no `role` column until Admin Settings/`024`); every query/action is unit/integration-tested directly, and e2e covers the real, current access-denial behavior. Visual QA (temporary local bypass across this feature's own page/actions, `014`'s reused `toggle-page-status.ts`/`save-content-page.ts`, and its own `/pages/[slug]` page — the Edit cross-link target — fully reverted) exercised stats, search, all four filters, the empty state, publish/unpublish, page creation, View/Edit navigation into `014`'s real inline-edit surface, and both delete-confirm paths (Yes and No) end-to-end with a zero-violation axe-core scan and no product bugs found.
22. Public profile page (`/u/:handle`) — 🚀 implemented (2026-07-14) — all 28 tasks across Setup/Foundational/US1(P1) view profile/US2(P2) follow+message+invite/US3(P3) in-common+report+block/Polish complete: the public `/u/:handle` page (no login required to view) — identity/bio, real computed stats (rating+review count, a `sessions` proxy = accepted applications as applicant + closed/full hosted postings), the real per-user-editable games list, currently-open hosted postings with an inline "Request" (reusing `006`'s `applyToPosting` directly), display-only Player reviews (new `reviews` table, no writer yet — rating submission stays deferred, `docs/future-work.md`, same "ship the entity/display now, adopt the writer later" pattern as `Notification`/`AuditEntry`), a public-info sidebar (region/age group/platforms), and — for an authenticated non-self viewer — a "You have in common" sidebar (mutual follows + shared games, both computed at read time). Drops six wireframe elements (online presence, reliability %, groups, per-game rank/hours, level, pronouns/languages/timezone) against already-established decisions. New Follow (`toggle-follow.ts`, a new `follows` table — hard-deleted on unfollow, no trust/safety value, same exception as `SavedListing`/`Likes`) and a host-initiated "Invite to a party" (`invite-to-party.ts`) that reuses `006`'s `applications` table via a new `initiatedBy` (`applicant`\|`host`) column rather than a parallel invite system — an invite still needs the INVITED user's own consent, so `011`'s `accept-request.ts`/`decline-request.ts`/`get-inbox-list.ts`/`get-conversation.ts` all gained a small, bounded authorization reversal (the invited applicant, not the inviting host, is who's authorized to accept/decline/view a host-initiated row), with `request-banner.tsx`/`conversation-list.tsx`/the inbox detail page's own display text branching to say "X invited you" instead of "X wants to join" for that direction. Found and fixed a real, previously-latent bug while wiring this up: `conversation-list.tsx`'s inbox row preview text was hardcoded to a generic "Wants to join your party" for EVERY request-kind item regardless of `item.preview`'s real value — silently discarding an applicant's own actual message on every request row, not just this feature's new invite items; fixed by rendering `item.preview` directly (which `get-inbox-list.ts` already computed correctly), with `e2e/inbox.spec.ts`'s own pre-existing assertion updated to match the now-correct real-message display. Also found and corrected a data-model.md field claim before it shipped: the spec named `users.gamesPlayed` (onboarding's flat array) as this feature's games source, but that field is set once at signup and never updated afterward — Profile's (`007`) own `userGames` table (via `addUserGame`/`removeUserGame`) is the real, currently-maintained source, so `get-public-profile.ts`/`get-in-common.ts` both read from `userGames` instead. No `requireRole`-style hardcoded gate blocks this feature (`requireVerifiedEmail`/`requireAuth` check real session state) — unlike every admin/moderation feature, it's fully exercised end-to-end through real Playwright sessions with no local bypass needed; `e2e/public-profile.spec.ts` covers all three user stories (view/not-found, follow/message/invite/accept across two real sessions/no-eligible-posting, mutual-connections/report/block, unauthenticated+unverified gating) plus a zero-violation axe-core scan.
23. News article detail (`/news/:slug`) — 🚀 implemented (2026-07-15) — all 28 tasks across Setup/Foundational/US1(P1) read article/US2(P2) like+save/US3(P3) keep-reading+share/cross-feature amendments to 013+020/Polish complete: the public `/news/:slug` article page (no login required) — category/date/computed-read-time meta, title, a fixed "playm8z team" byline (the wireframe's own; no per-article author tracking exists anywhere), a cover block, the full markdown body rendered via `marked`, tags, a "Keep reading" grid (up to 3 other live articles, reusing `013`'s own live-check query), a client-only `aria-hidden` reading-progress bar, and the reused newsletter-subscribe box. Read time is computed from the body's word count at render time (`013`'s own never-populated `readTimeMinutes` column stays unused dead weight, as anticipated). Like reuses Forum Thread's (`010`) already-polymorphic `likes` table as its third `targetType` (`newsPost`) — no schema change needed; Save gets its own small, separate `savedNewsPosts` table (deliberately NOT a premature generalization of `savedListings`, since this is only the second real consumer, below this project's own "generalize at three" bar), surfaced in Profile's (`007`) Saved tab as a new "Saved articles" section. Adds `newsPosts.slug` (unique, generated once at creation by a bounded amendment to Admin News' (`020`) `save-news-post.ts`, immutable afterward — the same numeric-suffix collision approach Admin Content Pages' `021` established) and bounded amendments to News feed's (`013`) own `NewsPostCard`/`FeaturedPost` components linking each card to its article. Found and fixed two real gaps beyond this feature's own explicit scope: (1) `newsPosts` had no `tags` column or editor field anywhere, despite spec.md's own FR-001 requiring tags to render — added `newsPosts.tags` plus a plain comma-separated tags input to Admin News' editor, matching Forum index's own established tags-input pattern (unlike `readTimeMinutes`, tags have no way to be computed from existing data, so a stored, moderator-set column was the correct shape, not a "leave it unpopulated" resolution); (2) `conversation-list.tsx` (Inbox, `011`) had a long-standing hardcoded fallback string discarding every request's real message in the inbox list view — caught while building this feature's own Like/Save error-display and fixed at the source. `requireVerifiedEmail`/`requireAuth` gate Like/Save/viewing Saved articles — real session state, not a hardcoded rank — so this feature (like Public Profile before it) needed no local QA bypass at all; `e2e/news-article-detail.spec.ts` exercises all three user stories fully through real sessions, including a zero-violation axe-core scan and a hydration-mismatch bug in the share buttons (caught and fixed by moving `window.location`-dependent share-intent construction out of render and into click handlers).
24. Admin Settings — 🚀 implemented (2026-07-15) — all 43 tasks across Setup/Foundational/US1(P1) general+maintenance/US2(P2) moderation+auto-flag/US3(P3) roles+features+safety/Polish complete: `/admin/settings`, gated at `admin` specifically — stricter than every other `/admin/*` page's `moderator` minimum, and the feature that finally ships the real `users.role` column (`user`\|`support`\|`viewer`\|`moderator`\|`admin`) every prior admin feature's own `require-role.ts` comment had been waiting on since Content Page (`014`) — so this is also the FIRST admin feature whose own gate (and every other admin feature's, retroactively) can be exercised by a real, non-bypassed session; `require-role.ts` now queries the real column fresh per request (never cached in the JWT) so a role change takes effect on the very next request. A tabbed client shell (`role="tablist"`, plain `useState` section switching, no URL param) over five sections, each a real, working save flow: **General** (site name/tagline/support email/default theme — no current reader, Design System infra out of scope — plus Error Pages' (`002`) long-deferred real maintenance-mode toggle, now with genuine sitewide effect); **Moderation & auto-flag** (the admin-editable banned-phrases list plus four independent filter toggles now drive `017`'s/`018`'s shared `auto-flag-rules.ts` instead of its own hardcoded constants; a computed — never stored — auto-hide-after-N-reports rule added as a second amendment to Home's/Browse's/Forum index's (`003`/`004`/`009`) queries, so resolving enough reports via any of `017`/`018`/`019`'s own actions automatically un-hides a row with zero extra code anywhere; a computed "needs ban review" display badge — never an automated ban — added to `017`'s/`018`'s/`019`'s own queue queries); **Roles & access** (team list with a live role dropdown/remove, plus "Invite a team member" — one `assignTeamRole` action serves both a known member's dropdown and a by-email lookup for a brand-new plain `user`, matching research.md's "no separate invite-token system" call); **Feature flags** (only Open Signups gets real enforcement — Credentials sign-up's `register/route.ts` — the other five persist but stay inert, `docs/future-work.md`); **Safety** (Discoverable-profiles-by-default, wired to both sign-up paths' account-creation moment). Extends `002`'s singleton `settings` table with all ~19 new fields exactly as that feature's own data-model.md anticipated, rather than a second competing config table. Found and fixed a real gap discovered mid-build, beyond the spec's own explicit list: an admin toggling maintenance mode on stayed unaffected only for `/admin/*`; every OTHER route (including the post-login `/continue` redirect and `/login` itself) still showed the maintenance page even to an authenticated admin, since `proxy.ts` never checked role at all before this feature — fixed with a sitewide admin bypass, plus a `/login` exemption so an admin who isn't currently signed in (a different browser, an expired session) can still reach the login form to fix it. Also found and fixed a second real bug via the new e2e suite itself: every settings-save Server Action called `revalidatePath()` (Next.js's own route cache) but never invalidated `get-settings.ts`'s separate hand-rolled 5-second TTL cache, so an admin saving and immediately reloading saw stale data for up to 5 seconds — `_resetSettingsCacheForTests` renamed to the now-production-purposed `invalidateSettingsCache()` and wired into the shared `upsertSettings()` helper every save action uses. A small, bounded retroactive fix closes the gap this feature's own research first surfaced: Public Profile (`022`) never honored Profile's (`007`) existing `showRegion`/`showAgeGroup` privacy toggles — `get-public-profile.ts`/the profile sidebar now conditionally omit those fields for a non-owner viewer. Every settings-save Server Action across all five sections logs an audit entry (`015`), closing the loop `002`'s own spec anticipated. Full unit/integration coverage (validations, `get-team.ts`, all 7 Server Actions — each proven against a real `admin` session AND a real `moderator` rejection, no mocking of the gate itself, a first for this admin cluster) plus `e2e/admin-settings.spec.ts` (11 tests, zero-violation axe scan, all through real sessions with real roles — no bypass of any kind, the first admin feature that needed none).
25. Moderator audit log — 🚀 implemented (2026-07-15) — all 21 tasks across Setup/Foundational/US1(P1) browse+search+filter/US2(P2) expand+export/US3(P3) close the 020+021 logging gap/Polish complete: `/admin/audit-log`, gated at `moderator` (deliberately less strict than Admin Settings' `024` admin-only gate — a read-only transparency tool, not a mutation surface) — the tenth `require-role.ts` consumer and, like Public Profile/News Article detail before it, needs no local bypass since the real role column is live. A real, server-side `searchParams`-driven search (actor/action/target/reason)/actor-filter (a live dropdown of every real actor who has ever logged, plus a "System" sentinel for a null `actorId`)/category-filter (the real, stored 4-value `moderation`\|`content`\|`access`\|`system`, never a fabricated finer classification per research.md #1) over `015`'s existing `auditEntries` — its first full, dedicated, filterable/paginated viewer (the dashboard's own feed only ever showed a short preview). Day-grouped Today/Yesterday/Earlier (one bucket further back than Notifications' own Today/Earlier, since this log is expected to be browsed further back); each row is a real, keyboard-operable disclosure (`aria-expanded`) revealing its `reason`/`meta` key-value detail; "Export CSV" is a real gated GET route handler (`/admin/audit-log/export`) re-running the exact same filter unpaginated, so what's on screen is what downloads. Closes a real, previously-unnoticed gap this feature's own design surfaced: Admin News (`020`) and Admin Content Pages (`021`) were the only two admin features that never wired `015`'s `logAuditEntry()` despite its own spec anticipating them — both now log a `content`-category entry on every publish/schedule/update (News) and create/publish/unpublish/delete (Content Pages). Full unit/integration coverage (`audit-log.ts` schemas, `get-audit-log.ts`'s search/filter/day-grouping/pagination, `export-audit-log-csv.ts`'s filter-fidelity, all four amended `020`/`021` actions each proven to call `logAuditEntry()` against a real seeded moderator) plus `e2e/audit-log.spec.ts` (11 tests, zero-violation axe scan, real moderator/non-moderator/anonymous sessions, both gap-fix scenarios exercised through the real Admin News/Admin Content Pages UI) — no bypass of any kind, same as Admin Settings' own suite.
26. Logged-out marketing landing page — 🚀 implemented (2026-07-15) — all 19 tasks across Setup/Foundational/US1(P1) real marketing page/US2(P2) CTA navigation/US3(P3) real hero-card data/Polish complete: closes the exact loop Home's (`003`) own spec left open — its root `page.tsx` now renders this feature's real marketing content for an unauthenticated visitor instead of redirecting to `/login`; an authenticated visitor's experience is completely unchanged. Every "live-feeling" number is real, computed at render time via a new `get-landing-stats.ts`: total players (`COUNT(user)`), games & tables (`COUNT(DISTINCT postings.game)`, across ALL postings ever — deliberately a catalog-breadth stat, not a "right now" one), parties formed this week (a new `applications.acceptedAt`, set by Inbox's, `011`, existing `accept-request.ts` alongside its `status='accepted'` write), and the hero's floating card(s) (1-2 real currently-open postings, reusing `listing-card.tsx` directly, with a clearly-decorative — never fabricated-looking — fallback when none exist). The wireframe's fake "4,300+ players online now" and "4.9★ avg teammate rating" are both dropped entirely (no real presence-tracking or rating-submission system exists), the same no-fake-data discipline already applied project-wide; the reworded "Real player profiles" feature card describes only what's real today, no reliability-score/live-rating claim; testimonials are the one deliberate, reasoned exception (curated marketing copy, not a real-time data claim). "Browse by genre" shows real per-genre open-posting counts across Browse's (`004`) own 8-genre enum, each linking into a real pre-filtered Browse view. Footer links About/Privacy/Terms to Admin Content Pages' (`021`) real seeded system pages; Community Guidelines/Careers/Safety Center point at plain not-yet-created slugs (Content Page's, `014`, real 404 until an admin creates them) — deliberately not seeded by this feature. Found and fixed one real bug during its own test-writing: postgres.js converts a JS `Date` differently than Postgres's own `defaultNow()`/`now()` for a "timestamp without time zone" column (a multi-hour skew), so a test comparing an explicit JS-Date-stamped row against a `defaultNow()`-stamped sibling can get the relative order backwards — fixed by giving both rows an explicit JS Date from the same clock, never mixing one explicit value with the schema's own server default when relative ordering matters. Full unit/integration coverage (`get-landing-stats.ts`'s three real stats via delta assertions against the shared, unscoped `users`/`postings`/`applications` tables — never a wholesale wipe of those tables, following the same discipline the audit log feature, `025`, established for a different reason; a component-level `landing-hero.test.tsx` for the zero-postings fallback, this project's first non-listing-card use of `@testing-library/react`'s established precedent; `accept-request.ts`'s new `acceptedAt` write) plus `e2e/landing-page.spec.ts` (14 tests, zero-violation axe scan) covering both root-route branches, real stats/hero-card/genre-counts, all CTAs, footer links, and `acceptedAt` propagating from a real Inbox accept action — no bypass of any kind needed.
27. Admin Users drawer — view full profile in a new tab — 🚀 implemented (2026-07-15) — all 7 tasks across a single US1(P1)/Polish complete: a small, scoped enhancement to already-shipped Admin Users (`016`), not a new top-level page. Adds one control to the existing per-user drawer (`user-drawer.tsx`) linking to that same user's real Public Profile (`022`, `/u/[handle]`) via the `handle` `get-user-detail.ts` already returns — opened with `target="_blank"` + `rel="noopener noreferrer"` so the admin queue's tab/state is never disturbed, and rendered unconditionally for active/flagged/banned users alike (a moderator cross-referencing a banned user's public profile is a real use case, not an edge case to hide). Explicitly not the new master-detail layout wireframe (reviewed and declined for now — logged to `docs/future-work.md`) and not bulk/multi-select management — the drawer's existing one-user-at-a-time model is unchanged. Also retires a stale header comment on `e2e/admin-users.spec.ts` claiming the drawer "can't be exercised end-to-end" — untrue since Admin Settings (`024`) shipped the real `role` column; this feature's own e2e coverage is the first to actually open the real drawer through a seeded `moderator` session and assert the new link's `href`/`target`/`rel`, for both an active and a banned target user.
28. Admin-only AI writing assist (News & Content Pages) — 🚀 implemented (2026-07-15) — all 24 tasks across Setup/Foundational/US1(P1) write from scratch/US2(P2) improve-rewrite/Polish complete: two new admin-only actions added to Admin News' (`020`) editor and the inline Content Page (`021`) editor at `/pages/[slug]`, calling Claude Haiku via the Vercel AI SDK + AI Gateway (`ADR 0007`) — a plain `"anthropic/claude-haiku-4.5"` model string, no direct Anthropic SDK, this project's first external-AI-provider integration. Gated at `admin` specifically (stricter than every other admin page's `moderator` minimum), reusing `require-role.ts`/`getCurrentRole()`. "Write from scratch" takes a short topic and returns a complete structured draft (News: title/excerpt/body; Content Page: a set of blocks) via `generateText`'s `output: Output.object()` (this SDK version has no standalone `generateObject`) — re-validated with the same Zod schema in `src/lib/ai/gateway.ts` itself rather than trusted solely to the SDK's internal check, a refinement surfaced by writing this feature's own mocked unit tests. "Improve/rewrite" is ONE shared, surface-agnostic Server Action (`improve-draft-text.ts`) for both surfaces — it always operates on plain text, reusing the Content Page editor's own existing `blockToText`/`withText` round-trip rather than a bespoke per-block schema. Neither action ever saves/publishes anything itself — both only populate the same draft form state a human typing would; every completed action logs an audit entry (`015`, `category: "content"`). Admin Forum was explicitly scoped out (no admin authoring surface exists there today — moderation-only) and logged to `docs/future-work.md`. e2e coverage (`admin-news.spec.ts`/`content-page.spec.ts`) proves the admin-only gate and control-availability state (including "Improve/rewrite" absent on empty text) through real seeded `admin`/`moderator` sessions — deliberately never clicking a request through to a real AI response in CI (that would mean every push makes real, billed, non-deterministic calls); the real end-to-end Gateway call was instead verified once, live, outside the test suite. Full Vitest coverage of all three Server Actions with `ai` mocked (role gate, input validation, audit logging, and — the one thing mocking exposed — that malformed AI output is actually rejected).
29. Real image upload for News post covers — 🚀 implemented (2026-07-15) — all 17 tasks across Setup/Foundational/US1(P1)/Polish complete: the Admin News (`020`) editor's Cover field, previously only 4 hardcoded gradient swatches (that feature's own explicit "no real imagery yet" scope line), now also accepts a real uploaded image via Vercel Blob (`ADR 0008`, `access: "public"`) — this project's first user-uploaded file. `newsPosts.cover` is reused as-is, no schema change: a new shared `newsCoverStyle()` helper (`src/lib/news/cover-style.ts`) distinguishes a gradient from a real image URL by string shape alone at render time, adopted by all 6 real consumers (News feed cards, the featured post, the article's own page, related-article lists, Profile's Saved tab, and the admin list's thumbnail) — a real DRY consolidation of six previously-duplicated inline styles, forced by the requirement that none of them regress for an already-published gradient-only post. Gated at `moderator` (not admin-only like feature 028's AI assist — this is a normal editing action, not a stricter one), via a new `upload-news-cover-image.ts` validating file type (JPEG/PNG/WebP) and a 5MB size cap with plain checks before ever calling Blob, rejecting anything else with the existing draft left completely untouched. Real Setup-phase gap found and closed: no Blob store existed on this project yet (`vercel blob list` confirmed `BLOB_STORE_ID` unset) — provisioned via `vercel blob create-store`, which (again) triggered a full local env-pull wiping `AUTH_SECRET`/`AUTH_GOOGLE_*`/`AI_GATEWAY_API_KEY` and replacing `DATABASE_URL` with Neon's, the identical `vercel integration add`-era risk recurring on a different command — restored immediately from values still in session context. e2e coverage (`admin-news.spec.ts`) proves only the upload control's presence for a real seeded moderator session, matching feature 028's own precedent of never exercising an external-call boundary Playwright can't intercept in CI; the real end-to-end Blob write (upload → public URL → fetches with the right content-type) was verified once, live, outside the test suite, then cleaned up. Full Vitest coverage of `upload-news-cover-image.ts` and `newsCoverStyle()` with `@vercel/blob` mocked.

30. Admin-editable genres — 🚀 implemented (2026-07-16) — the genre list behind Post a Game's chips, Browse's filter, and the landing page's genre counts moves out of a hardcoded `GENRES` const and into the `settings` singleton (`settings.genres text[]`), edited from a new **Lists** tab in Admin → Settings that reuses the Moderation tab's banned-phrases chip editor wholesale. The const survives only as `DEFAULT_GENRES`, the column default and seed — nothing reads it at request time, because a second runtime source would recreate exactly the drift this feature removes (FR-005: the two screens can never disagree). The real work was never the admin UI: `genre` was enforced by `z.enum(GENRES)` at **two** trust boundaries, and `z.enum()` needs its values at module-evaluation time, so membership can no longer live in the schema. Zod now checks shape and the actions check membership — **strictly on create, tolerantly on edit** (`manage-posting.ts` also accepts the genre a posting *already* stores, since applying create's rule there would strand every host whose genre an admin retired: they could never edit their own posting's title again without relabelling it). That pair has its own test file (`genre-membership.test.ts`) because it is the single most likely way to break the feature. Removing a genre never touches a posting (FR-007) — free by construction, since `postings.genre` is plain text with no enum, constraint, or FK. Browse deliberately changed behaviour: previously one unrecognised genre failed the whole `z.array(z.enum(...))` parse and `.catch([])` discarded the *entire* genre filter, so `?genres=FPS&genres=MOBA` with MOBA retired ignored FPS too; the page now intersects against the stored list, dropping only the unknown value. Two real traps found while building: three unrelated `get-settings` fixtures broke on the new required column (the cross-cutting risk the plan flagged), and the e2e initially seeded `settings` with a direct DB write — invisible to the dev server for up to 5s, since `get-settings.ts`'s TTL cache lives in the *server's* process and no test process can invalidate it, so the first test loaded a stale list and saved it straight back, silently dropping the seeded genre. The spec now drives the whole round trip through the UI.

31. Admin-editable suggested games — 🚀 implemented (2026-07-16) — the games offered at onboarding's "What do you play?" step move out of a `SUGGESTED_GAMES` const hardcoded *inside the client wizard component* and into `settings.suggestedGames text[]`, edited from a second section of feature 030's Lists tab (one tab, one save, one audit entry — 030's `save-lists-settings.ts` was extended to carry both arrays rather than a second action for the same form, and its chip editor extracted into a shared `ChipListEditor` at the point it would otherwise have been copy-pasted). Two findings from reading the code shaped it: **there is no validation coupling at all** — `gamesPlayedSchema` accepts any non-blank string and never checked this list (ADR 0001: games are free-text keywords, no curated catalog) — so removing a game has zero data and zero validation consequence, making this feature genuinely small; and **onboarding has no free-text game entry**, so the list *is* the entire choice set at that moment, which is why an empty list is refused (FR-009) where that rule would be arbitrary elsewhere. `suggested-games-not-a-catalog.test.ts` exists solely to make the tempting regression loud: validating `gamesPlayed` against the list would present as a correctness fix and would in fact build the catalog ADR 0001 rejects. A real Postgres trap found in Setup: `drizzle-kit push` does **not** escape apostrophes when generating an array default, so `.default([... "Baldur's Gate 3" ...])` emitted invalid DDL and the push died with `syntax error at or near "s"`, silently leaving the column absent — fixed with an explicit `sql\`ARRAY[...]::text[]\`` default using Postgres's doubled-quote escaping, and pinned by a test round-tripping the apostrophe, ampersand and colon entries (`Baldur's Gate 3`, `D&D 5e`, `Magic: The Gathering`). The `users.gamesPlayed`-vs-`userGames` split remains deliberately untouched (Principle IV) and stays logged as future work.

**All 31 features are now fully implemented and merged — the project-wide constitutional gate (v1.0.0) closed at spec/plan/tasks completion on 2026-07-12, and every tracked feature has now shipped real, working code.**

## Explicitly out of scope for now

See `docs/future-work.md` for the full list and reasoning — notably
Groups/Clans, a per-game hub page, password reset, and mobile-specific
layouts aren't on this list because they're deferred or still
undesigned and not currently being worked on.
